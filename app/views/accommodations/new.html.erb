<div class="container-new-accommodation">
  <div class="new-form-box row">
    <div class="new-form col-11 col-lg-8">
    <h2> Where did you stay in <%= @destination.city %>? </h2>
      <%= simple_form_for [@itinerary, @destination, @accommodation] do |f| %>
        <%= f.input :name%>
        <%= f.input :address%>
        <%= f.input :accommodation_type%>
        <div>
            <div class="button-cluster">
                <button type="button" id="custom-button-acc">📷 Upload photos</button>
                <span id="custom-text-acc">No photo chosen, yet.</span>
                <%= f.input :photos, as: :file, label: false, input_html: { multiple: true }%>
              </div>
              <div>
                <%= f.submit class:"search-button", style:"margin: 0px 0px 50px 0px;" %>
              </div>
          </div>
      <% end %>
    </div> 
  </div>
</div>
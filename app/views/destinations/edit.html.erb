<div class="container">
  <div class="new-form-box row">
    <div class="new-form col-12 col-lg-8">
    <h2> Which city did you visit on your trip? </h2>
      <%= simple_form_for [@itinerary, @destination] do |f| %>
        <%= f.input :country%>
        <%= f.input :city%>
        <%= f.input :date%>
        <%= f.input :description%>
        <%= f.input :photos, as: :file, input_html: { multiple: true }  %>
        <%= f.submit "Save changes",  class:"search-button" %>
        <%# <div>
          <div class="button-cluster">
            <button type="button" id="custom-button-dest">📷 Upload photos</button>
            <span id="custom-text-dest">No photo chosen, yet.</span> %>
            <%# <%= f.input :photos, as: :file, label: false, input_html: { multiple: true }%> 
          <%# </div>
          <div>
            <%= f.submit "Save changes",  class:"search-button" %>
          <%# </div> %>
        <%# </div> %>
      <% end %>
    </div>
  </div>
</div>